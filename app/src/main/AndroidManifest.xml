<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.m7uf2_projecte2_grup_beta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.M7UF2PROJECTE2GRUPBETA">
        <activity android:name=".Vista5"></activity>
        <activity android:name=".Vista3" />
        <activity android:name=".Vista4" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".EjemploBaseDeDatos" />
        <activity
            android:name=".Vista2"
            android:theme="@style/Theme.M7UF2PROJECTE2GRUPBETA.NoActionBar" />
        <activity
            android:name=".Vista1"
            android:label="@string/app_name"
            android:theme="@style/Theme.M7UF2PROJECTE2GRUPBETA.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        s
        <meta-data android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyCL-HBOAWStTd1s5kXDt9BJHZtgOgSVep0" />
    </application>

</manifest>